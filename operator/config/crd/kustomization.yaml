apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# The CRD is deployed in an EARLY argocd tier, ahead of the operator and of any
# lifecycle/activator app that creates AnkiInstance objects (ARCHITECTURE §13,
# requirements-operator §9). Keep it as its own app/manifest set.
resources:
  - bases/anki.ankimcp.ai_ankiinstances.yaml
